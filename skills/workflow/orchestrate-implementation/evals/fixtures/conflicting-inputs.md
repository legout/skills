# Conflicting input fixture

## ADR decision

The public export function must preserve its existing positional signature.

## Specification requirement

Replace the public export function with a new required options object.

## Acceptance

Do not begin implementation until the signature conflict is resolved by the owner.
