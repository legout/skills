{
  "skill_name": "orchestrate-implementation",
  "evals": [
    {
      "id": 1,
      "name": "plan-only-task-graph",
      "prompt": "Plan-only: read the feature plan. Produce a compact manifest summary, task dependency order, lane assignment, and two bounded worker briefs. Do not edit source files, create worktrees, commit, or use any orchestration skill.",
      "expected_output": "Keeps plan-only authority, identifies Task 2 as dependent on Task 1, and gives bounded briefs without pasting the whole plan.",
      "files": ["fixtures/feature-plan.md"]
    },
    {
      "id": 2,
      "name": "conflict-preflight",
      "prompt": "Preflight the conflicting input fixture without using an orchestration skill. Do not dispatch a worker. Report the exact conflict, why implementation must pause, and the owner decision required.",
      "expected_output": "Stops before mutation and identifies the incompatible public signature requirements.",
      "files": ["fixtures/conflicting-inputs.md"]
    },
    {
      "id": 3,
      "name": "multi-lane-review-policy",
      "prompt": "Explain how you would execute two independent implementation tasks with Pi sessions, worktrees, commits, reviewers, per-task test obligations, and optional persistent architecture/domain/quality peers. Include supervised and autonomous integration differences. Do not modify files and do not use an orchestration skill.",
      "expected_output": "Uses native subagents for lifecycle, intercom only for named read-only peers, one writer per worktree, focused TDD only for new-test tasks with existing-check/no-new-test alternatives, adaptive review with immediate review for high-risk work, branch-scoped lastReviewedSha..laneHeadSha review for each low-risk lane before fan-in, an exact candidate-branch review after assembly, and separate publication gates.",
      "files": []
    },
    {
      "id": 4,
      "name": "managed-lifecycle-native-acceptance",
      "prompt": "Executable native acceptance run (live execution currently PENDING): create a disposable throwaway Git repository for this evaluation — never use a real project — and use the orchestrate-implementation skill in autonomous mode with native Pi subagent tooling to execute fixtures/managed-lifecycle.md end to end (disposable candidate assembly is authorized; target integration is not): discover agents, pin the collision-checked named base, launch a small managed mutation worker from the named baseRef, allow normal worker finalization, move the parent HEAD, reconstruct and review the lane from the durable handoff patch and digest, launch a fresh fix worker and reconstruct its complete replacement patch from the same pinned base, then record a registered candidate handoff for merge-worktree. No push, PR, deploy, or release. Report BLOCKED with the exact error if any native prerequisite is unavailable; never fall back to a CLI or foreground mode.",
      "expected_output": "Either a live PASS with every declared evidence field populated (runtime version, discovered agents, run/lane ids, named base ref and resolved SHA, patch path and digest, worker-reported tree/cleanliness, finalization and cleanup state, parent movement, reconstructed review ref/sha/tree and exact review range with verdict, replacement digest with reset boundary, registered candidate path/base/head and handoff fields), or a fail-closed BLOCKED report naming the exact infrastructure blocker. Moved-base and digest-mismatch refusals create no review resources; corrupt-artifact and wrong-tree refusals create no reconstruction commit and preserve diagnostic artifacts. Failed required assertions produce FAIL; an owner-selected supervised pause produces AWAITING APPROVAL. Offline suite success is never substituted for this fixture. No CLI fallback or unrelated-project mutation is allowed; native managed worktrees and session artifacts outside the disposable root are permitted only with recorded paths and ownership. The live status stays explicitly PENDING until actually executed.",
      "files": ["fixtures/managed-lifecycle.md"]
    }
  ]
}
